package Topic5.databases;
/*
import Topic5.databases.model.Student;
import Topic5.databases.service.StudentService;

import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import java.util.List;

@RunWith(SpringRunner.class)
@SpringBootTest
public class StudentServiceUnitTest {

    @Autowired
    private StudentService bookService;

    @Test
    public void whenApplicationStartsThenHibernatesCreatesInitialRecords(){
          List<Student> students = bookService.list();

          Assert.assertEquals(students.size(), 3);
}

 */

